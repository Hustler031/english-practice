(() => {
  // Dynamic categories and PDF/source selectors will be populated from the Sources API.
  // New PDFs therefore require database registration, not frontend rewrites.
  window.EnglishSources = Object.freeze({});
})();
