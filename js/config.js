window.EnglishPracticeConfig = Object.freeze({
  // Public Apps Script Web App URL goes here after deployment.
  // Never place Google credentials, API keys, tokens or spreadsheet secrets here.
  apiBaseUrl: "",
  apiVersion: 1,
  requestTimeoutMs: 15000
});
