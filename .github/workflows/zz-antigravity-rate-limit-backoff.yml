name: One-shot Antigravity rate-limit backoff
on:
  push:
    branches: [english-antigravity-rate-limit-backoff-20260906]
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: english-antigravity-rate-limit-backoff-20260906
      - name: Patch rate-limit handling
        run: python .github/scripts/patch-antigravity-rate-limit-backoff.py
      - name: Commit source-only result
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/zz-antigravity-rate-limit-backoff.yml .github/scripts/patch-antigravity-rate-limit-backoff.py
          git add supabase/functions/_shared/english-antigravity-luna.ts .github/scripts/validate-english-hybrid-ai-content.cjs
          git commit -m "Honor Gemini retry hints in English AI pipeline"
          git push origin HEAD:english-antigravity-rate-limit-backoff-20260906