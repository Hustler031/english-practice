"use client";

import { FormEvent, useEffect, useState } from "react";
import { rpc } from "@/lib/supabase";
import { useAuthGuard } from "@/lib/use-auth";
import { EnglishLoading } from "@/components/english-frame";
type Saved={id:string;word:string;meaning:string;context:string;status:string;practiceQuestionId:string;gptStatus:string;captureType:string;resolvedType:string;created:string}; const types=["AUTO","V","SM","OWS","PV","IP"];
export default function SavedPage(){const ready=useAuthGuard();const [rows,setRows]=useState<Saved[]>([]);const [word,setWord]=useState("");const [context,setContext]=useState("");const [type,setType]=useState("AUTO");const [error,setError]=useState("");async function refresh(){setRows(await rpc<Saved[]>("english_get_saved_items"));}useEffect(()=>{if(ready)refresh().catch((e:any)=>setError(e.message));},[ready]);async function add(event:FormEvent){event.preventDefault();setError("");try{await rpc("english_save_word",{p_word:word,p_context:context,p_capture_type:type,p_module:"web-v2",p_source:"Manual capture"});setWord("");setContext("");setType("AUTO");await refresh();}catch(e:any){setError(e.message);}}async function changeType(id:string,next:string){setRows(rows=>rows.map(item=>item.id===id?{...item,captureType:next}:item));try{await rpc("english_set_saved_item_type",{p_saved_id:id,p_capture_type:next});await refresh();}catch(e:any){setError(e.message);await refresh();}}if(!ready)return <EnglishLoading text="Checking session…"/>;return <><section className="page-intro"><h1>My Saved Words</h1><p>Your own words, doubts and usage points.</p></section><form className="card stack" onSubmit={add}><input className="input" value={word} onChange={e=>setWord(e.target.value)} placeholder="Word / doubt / usage point" required/><input className="input" value={context} onChange={e=>setContext(e.target.value)} placeholder="Context (optional)"/><div className="capture-types">{types.map(item=><button type="button" key={item} className={`capture-type ${type===item?"selected":""}`} onClick={()=>setType(item)}>{item==="IP"?"I/P":item}</button>)}<span className="spacer"/><button className="btn primary">Save</button></div>{error&&<div className="error-box">{error}</div>}</form><section className="section-block"><div className="study-list">{rows.map(item=><article className="saved-row" key={item.id}><div><b>{item.word}</b><small>{item.meaning||item.context||"Pending enrichment"}</small></div><span className="pill">{item.gptStatus}</span><div className="capture-types">{types.map(next=><button key={next} className={`capture-type ${item.captureType===next?"selected":""}`} onClick={()=>changeType(item.id,next)}>{next==="IP"?"I/P":next}</button>)}</div></article>)}</div></section></>;}
