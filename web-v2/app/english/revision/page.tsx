"use client";
import Link from "next/link";
import { useCallback,useEffect,useState } from "react";
import QuizRunner from "@/components/quiz-runner";
import { EnglishLoading } from "@/components/english-frame";
import { rpc } from "@/lib/supabase";
import { useAuthGuard } from "@/lib/use-auth";
type Hub={due:number;weak:number;persistentWeak:number;difficult:number;starred:number;seenBefore:number;mastered:number;learning:number};type Pick={mode:"due"|"weak"|"recall"|"difficult";label:string;count:number};type Pending={mode:Pick["mode"];label:string;available:number};
export default function RevisionHome(){const ready=useAuthGuard();const[hub,setHub]=useState<Hub|null>(null);const[pick,setPick]=useState<Pick|null>(null);const[pending,setPending]=useState<Pending|null>(null);const[error,setError]=useState("");useEffect(()=>{if(ready)rpc<Hub>("english_get_revision_hub").then(setHub).catch((e:any)=>setError(e.message))},[ready]);const load=useCallback(()=>pick?rpc<any[]>("english_get_revision_batch",{p_mode:pick.mode,p_count:pick.count}):Promise.resolve([]),[pick]);if(!ready)return <EnglishLoading text="Checking session…"/>;if(pick)return <QuizRunner title={pick.label} backHref="/english/revision" load={load} module="revision" onExit={()=>setPick(null)}/>;const open=(mode:Pending["mode"],label:string,n:number)=>n>0&&setPending({mode,label,available:n});return <div className="top-level-parity"><section className="page-intro"><h1>Revision</h1><p>Review only what needs attention.</p></section>{error&&<div className="error-box">{error}</div>}<section className="section-block"><h2 className="section-cap">Needs Attention</h2><div className="legacy-list"><button className="legacy-row" disabled={!hub?.due} onClick={()=>open("due","Due Today",hub?.due||0)}><span className="legacy-row-copy"><b>⏰ Due Today</b><small>Spaced revision</small></span><span>{hub?.due??"—"} ›</span></button><button className="legacy-row" disabled={!hub?.weak} onClick={()=>open("weak","Weak / Wrong",hub?.weak||0)}><span className="legacy-row-copy"><b>🔥 Weak / Wrong</b><small>Persistent Weak → Weak → Fragile</small></span><span>{hub?.weak??"—"} ›</span></button><Link className="legacy-row" href="/english/starred"><span className="legacy-row-copy"><b>⭐ Starred Revision</b><small>Day-wise central starred review</small></span><span>{hub?.starred??"—"} ›</span></Link><button className="legacy-row" disabled={!hub?.difficult} onClick={()=>open("difficult","Difficult Revision",hub?.difficult||0)}><span className="legacy-row-copy"><b>⚡ Difficult</b><small>Questions you manually marked difficult anywhere</small></span><span>{hub?.difficult??"—"} ›</span></button><button className="legacy-row" disabled={!hub?.seenBefore} onClick={()=>open("recall","Seen Before / Recall",hub?.seenBefore||0)}><span className="legacy-row-copy"><b>↻ Seen Before</b><small>Rotate previously encountered questions</small></span><span>{hub?.seenBefore??"—"} ›</span></button></div></section><section className="section-block"><h2 className="section-cap">Personal Revision</h2><div className="legacy-list"><Link className="legacy-row" href="/english/saved"><span className="legacy-row-copy"><b>🔖 My Saved Words</b><small>Smart Revision · day-wise personal history</small></span><span className="legacy-chevron">›</span></Link><Link className="legacy-row" href="/english/mastered"><span className="legacy-row-copy"><b>✓ Mastered / Don’t Repeat</b><small>{hub?.mastered??0} excluded items · restore anything marked by mistake</small></span><span className="legacy-chevron">›</span></Link></div></section>{pending&&<div className="sheet-backdrop" onMouseDown={e=>{if(e.target===e.currentTarget)setPending(null)}}><div className="legacy-sheet"><h3>{pending.label}</h3><div className="legacy-muted">{pending.available} available · how many questions?</div><div className="legacy-counts">{[10,20,30,50].map(n=><button key={n} onClick={()=>{setPick({mode:pending.mode,label:pending.label,count:n});setPending(null)}}>{n}</button>)}</div><button className="btn ghost full-width" style={{marginTop:12}} onClick={()=>setPending(null)}>Cancel</button></div></div>}</div>}
